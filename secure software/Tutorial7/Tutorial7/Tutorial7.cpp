
using namespace std;
#include <iostream>


uint16_t aFunction(float x)
{
    cout << "Number is: " << x << endl;
    return 40000;
}
int main()
{
    int arr[10];
    int* p2 = arr;
    float a = 5.9f;
    uint16_t g;
    g = aFunction(a);
    //cout << "g is:" << g << endl;
        system("pause");
    for (int i = 0; i < -1; ++i) { ; }
    return 0;
}